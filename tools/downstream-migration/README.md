# Downstream 2.0 Migration Consumer

This unpublished crate exercises the public 2.0 migration surface without
workspace feature unification. It is verification infrastructure, not a crate
intended for publication.

Run it with:

```bash
cargo test --manifest-path tools/downstream-migration/Cargo.toml
```
